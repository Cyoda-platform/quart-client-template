{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cyoda.com/cloud/event/search/EntityMetadata.json",
  "title": "EntityMetadata",
  "description": "Metadata about an entity. id, modelKey and creationDate are invariant against the point-in-time. All other values are with respect to the as-at point-in-time for which the entity was retrieved. If the point-in-time was not explicitly set, the values correspond to the latest state of the entity.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "ID of the entity.",
      "format": "uuid"
    },
    "modelKey": {
      "$ref": "../common/ModelSpec.json",
      "description": "Model of the entity."
    },
    "state": {
      "type": "string",
      "description": "The state of the entity at the given point-in-time."
    },
    "creationDate": {
      "type": "string",
      "format": "date-time",
      "description": "The creation date of the entity."
    },
    "lastUpdateTime": {
      "type": "string",
      "format": "date-time",
      "description": "The last time the entity was updated as-at the given point-in-time. Equals the creation date if the entity has not been updated."
    },
    "pointInTime": {
      "type": "string",
      "format": "date-time",
      "description": "Optional value for the as-at point-in-time for which the entity was retrieved."
    },
    "transitionForLatestSave": {
      "type": "string",
      "description": "The transition applied of the entity when last saved as-at the given point-in-time."
    },
    "transactionId": {
      "type": "string",
      "format": "uuid",
      "description": "The transaction id of the entity when last saved as-at the given point-in-time."
    }
  },
  "required": [
    "id",
    "state",
    "creationDate",
    "lastUpdateTime"
  ]
}